{
  "name": "germ-warfare",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "세균전 (Ataxx) — client-side PvE web game demo",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test src/game/test/*.test.mjs"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
